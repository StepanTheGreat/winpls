# winpls
A window, please.

Like `miniquad`, but uses `winit` and `wgpu` under the hood. 
The primary idea is to simplify window and graphics context management. If common issues (like error detection) can be solved - they should be
the responsibility of this layer.

## Licensing
Under your choice: MIT or Apache-2.0